# PostgreSQL (any provider). Server has SSL disabled → DATABASE_SSL=false.
DATABASE_URL=
DATABASE_SSL=false
# Optional: API base URL for the client (leave blank to use same origin / dev proxy).
VITE_API_URL=
